---
description: List all React component files in the components folder, optionally filtered by subdirectory
---

# List Components

## Task

List all React component files (`.tsx`, `.ts`, `.jsx`, `.js`) in the components folder.

If a subdirectory argument is provided, only list files in that subdirectory.

Check `/components/` first (not `/src/components/`).

## Output Format

- Numbered list of files with relative paths
- Brief one-line description of each (infer from filename)
- Summary count at the end

If no files found, say "No components found."
